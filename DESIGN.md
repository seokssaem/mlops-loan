# Loan Lab 디자인 시스템

> 이 문서는 화면을 만들기 전에 디자인 판단의 기준을 고정하기 위해 작성했습니다.
> 학생들은 완성된 CSS보다 먼저 이 문서가 어떤 결정을 내렸는지 살펴보세요.

## 1. 프로젝트 목표

Loan Lab은 대출 승인 예측 모델을 직접 사용해보는 교육용 인터페이스입니다.

- 13개의 입력을 부담 없이 이해하고 완료할 수 있어야 합니다.
- 결과를 실제 금융기관의 결정이 아닌 **모델의 학습용 예측**으로 표현해야 합니다.
- 브라우저가 어떤 JSON을 API에 보내고 받는지 학생이 확인할 수 있어야 합니다.
- 데스크톱과 모바일에서 같은 정보 우선순위를 유지해야 합니다.

핵심 문장:

> 차분한 금융 신뢰감과 학생용 ML 투명성을 한 화면에 담는다.

## 2. 레퍼런스 조사

Inspo가 제안하는 방식처럼 실제 서비스에서 반복되는 원칙을 관찰했습니다. 특정 화면의 색이나 배치를 그대로 복제하지 않았습니다.

| 레퍼런스 | 관찰한 원칙 | Loan Lab에 적용한 방식 |
| --- | --- | --- |
| [Toss 대출](https://toss.im/service/loan) | 넓은 여백, 짧은 생활어, 한 번에 하나의 행동 | 히어로의 메시지와 첫 CTA를 하나로 제한 |
| [카카오뱅크 주택담보대출](https://m.kakaobank.com/products/mortgageLoan) | 핵심 정보를 먼저 요약하고 긴 내용을 순서대로 설명 | 결과에서 확률·등급·입력 요약을 먼저 표시 |
| [케이뱅크 신용대출](https://www.kbanknow.com/web/product/loan/credit-loan) | 단위가 분명한 정보 구조와 불안을 줄이는 안내 | 만원·개월·점 단위를 입력 필드에 고정하고 신뢰 패널 배치 |
| [Upstart](https://www.upstart.com/) | 정보 입력부터 결과까지 단계가 명확한 흐름 | 13개 항목을 3단계로 나누고 진행 상태 표시 |
| [Nubank 개인대출](https://nubank.com.br/nu/emprestimos/pessoal) | 단순함과 투명성을 가치로 설명 | 모델 한계, 편향, 개인정보 안내를 결과와 함께 제공 |

### 관찰 → 번역 원칙

```text
실제 화면 관찰
    ↓
반복되는 디자인 원칙 추출
    ↓
교육용 ML 서비스의 목적과 제약에 맞게 번역
    ↓
디자인 토큰과 컴포넌트로 구현
```

레퍼런스를 사용한다는 것은 화면을 베끼는 일이 아닙니다. 여백, 정보 위계, 그룹화, 상태 표현 같은 **디자인 문법을 문제에 맞게 다시 쓰는 일**입니다.

## 3. 시각 방향

### 피한 것

- 의미 없이 사용한 보라색 그래디언트
- 모든 정보를 둥근 카드 안에 넣는 구성
- 결과를 축하하거나 위협하는 과한 애니메이션
- 실제 대출 승인으로 오해할 수 있는 확정적 문구
- 장식용 스톡 사진과 외부 이미지 의존성

### 선택한 것

- 정보 위계가 분명한 밝은 캔버스와 진한 잉크 색상
- 행동과 진행 상태에만 사용하는 코발트 블루
- 시스템 상태에 사용하는 절제된 민트 컬러
- 큰 제목, 넉넉한 여백, 정교한 구분선
- 모든 그래픽을 CSS로 표현하는 오프라인 친화적 구조

## 4. 디자인 토큰

### 색상

| 역할 | 값 | 사용 |
| --- | --- | --- |
| Ink | `#101828` | 제목, 주요 본문, 다크 패널 |
| Ink Soft | `#344054` | 보조 본문 |
| Muted | `#667085` | 도움말, 메타데이터 |
| Canvas | `#F5F7FA` | 페이지 배경 |
| Paper | `#FFFFFF` | 폼과 결과 패널 |
| Border | `#E4E7EC` | 구분선, 입력 테두리 |
| Primary | `#315EFB` | 주요 행동, 포커스, 진행 상태 |
| Primary Soft | `#EEF2FF` | 선택 상태 배경 |
| Success | `#14B87A` | API 준비, 상대적으로 낮은 위험 |
| Warning | `#B54708` | 주의 상태 |
| Error | `#C4320A` | 오류 문구 |

승인과 미승인은 색만으로 구분하지 않습니다. 제목, 설명, 등급 문자를 항상 함께 표시합니다.

### 타이포그래피

- 기본 글꼴: `Pretendard`, `Noto Sans KR`, OS 시스템 글꼴 순서
- 장식용 숫자/강조: `Georgia`
- Hero: `48–78px / 1.04 / 700`
- Section title: `34–52px / 1.16 / 700`
- Body: `16–18px / 1.7–1.8`
- Label: `13px / 700`
- Metadata: `9–11px / 800 / 넓은 자간`

외부 웹 폰트를 불러오지 않습니다. 수업장 네트워크가 없어도 같은 정보 위계가 유지됩니다.

### 간격과 형태

- 8px 기반 간격 체계
- 콘텐츠 최대 폭: `1180px`
- 섹션 상하 여백: 데스크톱 `112px`, 모바일 `78px`
- 카드 반경: `16px`, 큰 시각 패널 `24px`
- 입력 높이: `50px`
- 주요 터치 영역: 최소 `44px`

## 5. 정보 구조

```text
헤더
└─ 히어로: 서비스 목적 + 단일 CTA
   └─ 신뢰 스트립: 저장/조회/교육용 고지
      └─ 3단계 입력 폼
         ├─ 신청자 정보
         ├─ 재무 정보
         └─ 대출 조건
            └─ 결과: 확률 + 등급 + 입력 요약 + API 원문
               └─ 설계 과정: Research → System → Build → Verify
```

### 13개 필드의 그룹화

1. **신청자 정보** — 나이, 성별, 근속연수, 주거형태
2. **재무 정보** — 연소득, 카드 사용액, 신용점수, 기존 대출 건수, 부채비율
3. **대출 조건** — 신청 금액, 기간, 목적, 상환 방식

백엔드 필드 순서를 그대로 나열하지 않고 사용자가 질문의 맥락을 이해할 수 있는 순서로 재배치했습니다. API로 전송할 때는 원래 필드 이름을 유지합니다.

## 6. 컴포넌트 규칙

### 입력 필드

- 단위를 입력창 내부에 계속 표시합니다.
- 범주형 값은 학습 데이터에 없는 값이 전송되지 않도록 `select` 또는 라디오 버튼을 사용합니다.
- 도움말은 “무엇을 입력하는가”보다 “어떤 기준인가”를 설명합니다.
- Pydantic 스키마와 같은 최소·최대 범위를 HTML 속성에 반영합니다.

### 버튼

- 한 단계에는 주요 버튼 하나만 둡니다.
- 로딩 중 버튼을 비활성화하여 중복 요청을 막습니다.
- 이전 버튼은 흰색 보조 스타일로 표현합니다.

### 결과 패널

- `학습용 AI 예측 결과` 배지를 먼저 표시합니다.
- “승인/거절” 대신 “승인 가능성이 높게/낮게 예측”됐다고 표현합니다.
- 확률은 모델 출력값이며 실제 승인 가능성을 보장하지 않는다고 설명합니다.
- 요청 ID와 실제 JSON 응답을 제공해 추적 가능성을 수업에서 설명할 수 있게 합니다.

## 7. 상태 설계

| 상태 | 화면 반응 |
| --- | --- |
| 초기 | 기본 예시값과 단계 안내 표시 |
| 입력 오류 | 브라우저 기본 검증 + 폼 상단의 이해 가능한 문장 |
| 로딩 | 제출 버튼 비활성화, `예측 중…` 표시 |
| 승인 분류 | 가능성이 높게 예측되었다는 문장과 모델 등급 표시 |
| 미승인 분류 | 가능성이 낮게 예측되었다는 중립적 문장 표시 |
| 422 | 선택 항목과 숫자 범위를 확인하도록 안내 |
| 503 | 모델이 준비되지 않았음을 안내 |
| 네트워크 오류 | API 실행 상태를 확인하도록 안내 |

## 8. 반응형 규칙

- `900px` 이하: 폼과 설명 패널을 한 열로 전환합니다.
- `640px` 이하: 라벨 없는 숫자형 단계 표시, 한 열 입력, 하단 고정형 단계 버튼을 사용합니다.
- 결과 확률 링은 제목 위로 이동합니다.
- 설계 과정 카드는 한 열로 전환합니다.

## 9. 접근성과 책임 있는 표현

- 모든 입력에 명시적 `label` 또는 `legend`를 연결합니다.
- 키보드 포커스가 보이도록 블루 포커스 링을 제공합니다.
- 결과 컨테이너에 `aria-live="polite"`를 사용합니다.
- 장식 요소는 보조기술에서 제외합니다.
- `prefers-reduced-motion`을 존중합니다.
- “학습용”, “실제 금융 심사 아님”, “데이터 저장 안 함”을 반복해 명시합니다.
- 입력 예시는 실제 개인정보가 아닌 가상값만 사용합니다.

## 10. 구현 검수 체크리스트

- [ ] 13개 UI 필드가 `LoanRequest`와 정확히 일치하는가?
- [ ] 범주형 값이 학습 데이터의 인코더 범주와 일치하는가?
- [ ] 0–1 확률을 0–100%로 올바르게 변환하는가?
- [ ] 로딩 중 중복 제출이 방지되는가?
- [ ] 422, 500, 503, 네트워크 오류가 문장으로 보이는가?
- [ ] 승인과 미승인이 색 외의 정보로 구분되는가?
- [ ] 모바일에서 폼과 버튼이 잘리지 않는가?
- [ ] 키보드만으로 입력과 제출이 가능한가?
- [ ] 실제 금융 결정으로 오해할 표현이 없는가?


